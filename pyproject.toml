[project]
name = "forzatech-wrapper"
version = "0.1.0"
description = "TUI wrapper for ForzaTech-extraction-tools to convert Forza car models to Blender"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "textual==1.0.0",
    "python-dotenv==1.1.0",
]
